# Telegram-бот
aiogram>=3.7,<4.0

# HTTP-запросы и парсинг обычных (статических) сайтов
aiohttp>=3.9
beautifulsoup4>=4.12

# Парсинг динамических сайтов (Avito и т.п.)
playwright>=1.44

# Хранение уже увиденных объявлений (SQLite, async)
aiosqlite>=0.20

# Планировщик запуска парсинга по расписанию
APScheduler>=3.10

# Чтение токенов из .env
python-dotenv>=1.0
